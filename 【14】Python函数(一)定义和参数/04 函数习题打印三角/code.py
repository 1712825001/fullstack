# 编写一个函数，能够接受至少2个参数，返回最大值和最小值
# def double_values(*nums):
#     return max(nums), min(nums)
#
# print(double_values(*(range(100))))
# print(*double_values(*(range(100))))

# 编写一个函数，接受一个参数n，左右两种方法打印，要求数字必须对齐